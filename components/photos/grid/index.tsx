import { CardColumns } from "react-bootstrap";
import styled from "styled-components";

const Grid = styled(CardColumns)`
  margin: 0 auto;
  width: 100%;
  img {
    max-height: auto;
  }
`;

export default Grid;
