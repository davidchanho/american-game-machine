import Breakpoints from "./breakpoints";
import Colors from "./colors";

export const theme = {
  colors: {
    primary: Colors.primary,
    white: Colors.white,
    dark: Colors.dark,
  },
  breakpoints: {
    xs: Breakpoints.xs,
    sm: Breakpoints.sm,
    md: Breakpoints.md,
    lg: Breakpoints.lg,
  },
};
